#include<bits/stdc++.h>
using namespace std;

int main(){

 int n;
 cin>>n;
 set<int> s;

 for(int i = 0; i < n;++i){
    s.insert(12);
 }

 for(int i = 0; i < n;++i){
    auto it = s.lower_bound(12); //Maps ke case may  m.lower_bound hoga
                                 //sets ke case may  s.upper_bound hoga
 }                               //Maps ke case may  m.upper_bound hoga
 }

 


}